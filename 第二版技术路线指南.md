# ResearchClaw 第二版技术路线指南

本文档定义 ResearchClaw 第二版（v2）的目标、范围、要实现的指标，以及面板最终呈现效果。

来源依据：

- `第一版锐评.md`（v1 的架构债与优先级 P0–P3）
- `科研助手架构批判分析.md`（contract / state / evidence 三件事，证据闭环，带回边状态机）
- `OpenClaw-first-demo开发指南.md`（分层边界、phase、gate、artifact schema）
- 本轮已对齐的三项决策（见下）

## 0. 本版三项已确认决策

1. **范围：一版到位。** v2 同时做掉锐评的 P0/P1 后端硬骨头 + 真实 CLI 接入 + 新面板。CLI 不建在死路状态机上。
2. **人机对话：一问一答 headless。** 点击面板上的模型按钮 → 发一条消息 → CLI 跑一轮 → 结果回面板。不做嵌入式交互终端。
3. **CLI 范围：先只接 Claude Code，默认 Haiku。**
   - 默认模型 `claude-haiku-4-5`，**禁止默认用 Opus**。调试不烧钱是硬约束。
   - Gemini / Codex 在面板上**占位**，明确标注「未接入 / mock」，不在 v2 实现。

## 1. 一句话目标

```text
v2 要证明：ResearchClaw 能用真实 Claude Code 跑通一条结构化、可校验、可回退的科研链路，
过程对人类实时可见，人类能在关键节点 approve / revise，也能随时点开某个 CLI 一问一答，
而所有「结论」始终来自经过 gate 的结构化 artifact，绝不来自对话的事后提炼。
```

v1 证明的是「mock 骨架能跑通」。v2 要证明「真实模型 + 机器校验 + 证据闭环 + 实时可视 + 可回退」。

## 2. 核心原则（继承 + 新增）

继承 v1 的硬边界（开发指南 §8.1），一条都不松：

```text
gateway 不决定科研阶段
adapters 不改 research state
workflows 不绕过 contract
engine 不直接调用模型（只经 adapter）
OpenClaw 源码默认不改
```

v2 新增三条原则：

### 2.1 两通道模型（v2 最重要的红线）

CLI 产出的信息分两条流，**物理分开，永不混淆**：

| 通道 | 是什么 | 存成 | 面板位置 | 能否当结论 |
| --- | --- | --- | --- | --- |
| **过程流（transcript）** | CLI 整段对话 / 动作日志 | `raw_log` artifact（append-only） | 右栏实时 feed | ❌ 仅过程透明度 |
| **交付产物（artifact）** | CLI 被要求显式产出、符合 `output_schema` 的结构化输出 | `artifact`，经 gate 校验 | 中栏契约 / 证据 / 关键产物 | ✅ 唯一可信结论 |

**结论永远来自结构化 artifact + gate，绝不来自「从对话里提炼关键信息」。** 一旦 evidence 来自对 chat 的事后提炼，机器可校验契约、gate、证据闭环三样护城河全部失效。

### 2.2 CLI 即 adapter

engine 仍是唯一状态源，Claude Code 只是某个 phase 的执行器，或人机一问一答的后端。`ResearchOrchestrator` 注入的 adapter 从 `MockModelAdapter` 扩展为可选 `ClaudeCodeAdapter`，`ModelAdapter` 接口签名不变，workflow 层零改动。

### 2.3 mock 永远是回放底座

CLI 非确定、慢、会失败。所有 gate / 状态机测试和 `demo:fixture` 必须仍能在纯 mock 下跑通。真实 CLI 是按 phase / 按需 opt-in，不是替换 mock。

## 3. 范围

### 3.1 必须做（v2 范围）

**后端（承接锐评 P0/P1）**

- P0-1 `blocked` 可恢复：补 transition table 缺的回边，gate fail 优先回退到可重跑阶段，`blocked` 仅作兜底并提供 recover 入口。
- P0-2 `revise` 真重跑：feedback 作为新 input 再跑一次 contractDraft，产出内容真的不同的 v2 草稿。
- P0-3 idea_generation 门槛抽进 `gates.js`，统一 gate 入口。
- P1-4 新增 `claimEvidenceGate`：summary 前逐条校验 `contract.claim_evidence_map` 的每条 claim 是否都能在已存 artifact 找到对应 evidence，缺失则不准进 success summary；`evidence_index` 由真实映射生成。
- P1-5 关键 gate 从「非空」升级到「最低质量」（见 §6.3）。

**CLI 接入**

- `ClaudeCodeAdapter`：headless 调用 Claude Code，默认 Haiku，支持「workflow 模式」（结构化产物）与「consult 模式」（人机一问一答）。
- 沙箱隔离：每次 CLI 运行限定在独立工作目录 / git worktree，受限 `--allowedTools`。
- 成本护栏：默认模型、token / turn 上限、单次超时（见 §7）。

**实时面板（新 UI）**

- 三栏面板（见 §5），实时显示流程进度、契约、证据映射、关键产物、CLI 过程流。
- 实时推送通道（SSE），不靠纯轮询。
- 点击 artifact 看详情；点击 Claude 按钮一问一答对话；Gemini / Codex 按钮占位。

### 3.2 明确不做（留给 v3+）

- 不接 Gemini CLI / Codex CLI（仅占位）。
- 不做嵌入式交互终端（只做一问一答）。
- 不做 implementation / experiment / 真实复现执行阶段（phase enum 预留，逻辑不实现）。
- 不做多 worker 并行、多 agent 互投票。
- 不做分层长期记忆库（记忆库 nav 占位）。
- 不引入数据库（仍文件系统 evidence store）。

## 4. 架构变化

```text
OpenClaw hook / panel action
  -> Research Gateway / HTTP API
  -> Research Orchestrator（唯一状态源）
       |-- workflow 模式 --> ClaudeCodeAdapter(headless, haiku, forced schema) --> 结构化 artifact --> gate
       |-- consult 模式 --> ClaudeCodeAdapter(headless, haiku, free-form, --resume) --> raw_log
  -> Evidence Store（artifact / raw_log / state）
  -> Event Bus --> SSE --> 面板实时更新
```

新增模块：

- `adapters/claudeCode.js`：`ClaudeCodeAdapter`。
- `engine/events.js`：进程内事件总线（phase change / artifact / gate result / cli chunk / consult message）。
- `server.js`：新增 SSE endpoint + consult endpoint。
- `sandbox/`：CLI 运行目录管理与清理。

### 4.1 ClaudeCodeAdapter 两种模式

**workflow 模式（结构化产物，engine 驱动）**

```bash
claude -p "<task prompt + 要求把结果写入 ./out.json，严格符合给定 schema>" \
  --model claude-haiku-4-5 \
  --output-format stream-json \
  --allowedTools "Read,Write,WebSearch" \
  --max-turns 20 \
  --cwd <sandbox-dir>
```

流程：构造 prompt + `output_schema` + 沙箱目录 → spawn → stream-json 事件实时推送面板（raw_log）→ 运行结束读 `out.json` → 用对应 gate 校验 → 通过则 commit artifact，否则 block / 回退。**schema 由「要求写文件 + 读文件校验」保证，不靠解析自然语言。**

**consult 模式（人机一问一答，人驱动）**

```bash
claude -p "<人输入的消息>" \
  --model claude-haiku-4-5 \
  --output-format stream-json \
  --resume <session_id>   # 维持该 CLI 会话的多轮上下文
```

流程：人在面板点 Claude 按钮发消息 → headless 跑一轮 → 文本流式回面板 → 整轮存为 `raw_log`（标注 `consult`）。**consult 不自动推进 state**；若人觉得某次 consult 结果值得采纳，需显式「提升为 artifact」（人工 promote），才进入证据链。

## 5. 面板呈现效果

整体：三栏深色控制台。**视觉权重原则**——契约 / 证据 / 流程进度居中心 C 位（这是项目护城河），CLI 过程流作为「过程透明度」侧栏，不喧宾夺主。概念图右栏的「多模型对话」保留，但定位为「看 CLI 在干什么 + 随时插话」，不是「看三个 AI 聊天」。

### 5.1 左栏：导航 + 项目

| 区域 | 显示 | 数据来源 | 交互 |
| --- | --- | --- | --- |
| 工作空间切换 | 工作空间名 | 静态/配置 | 切换 |
| 导航 | 概览 / 研究工作流 / 文献库* / 实验管理* / 记忆库* / 设置（*占位） | 静态 | 概览、研究工作流可点；占位项灰显 |
| 当前研究 | 标题、运行中、已用时 | `state.phase` + `created_at` | 点击进入 |
| 历史研究 | 项目列表 + 状态（已完成 / 已终止） | `listProjectSummaries` | 点击切换；「已终止」项可点 recover（P0-1） |
| OpenClaw Gateway | 连接状态 | 健康检查 | — |
| 最近事件 | User Query / Tool / Web Search… | `state.signals` 尾部 | 查看全部 |

### 5.2 中栏：主流程 + 契约（C 位）

**研究工作流进展**
- 横向 phase 节点 pipeline + 总体进度 %。
- 数据来源：`state.phase` + `phase_history` + `gates`。节点状态：已完成（gate pass）/ 进行中 / 待运行 / **失败可回退**（P0-1 后 blocked 不再是死灰，显示「需修订，可回退到 X」）。
- 总体进度 % = 已 pass 阶段数 / 总阶段数。

**当前阶段卡片**
- 阶段名 + 状态 + 子任务清单 + 开始时间 + 关键产物预览（结构化 artifact 的 JSON 片段）。
- 数据来源：当前 phase 对应 artifact `content`。
- 交互：「查看完整产物」→ 打开 artifact 详情。

**契约 / 证据 tab 区**（四个 tab，对应概念图）
- `研究契约`：研究问题、假设、成功标准、失败信号、关键指标、数据划分。来源：approved contract `content`。
- `阶段状态`：每个 phase 的 gate 结果与 artifact ref。来源：`phase_history`。
- `证据映射`：**逐条展示 `claim_evidence_map` 的 claim → 实际命中的 artifact**，缺口高亮（P1-4 的前端出口）。来源：`claimEvidenceGate` 的计算结果。**这是必须填真数据的 tab，不允许写死。**
- `变更历史`：contract 各版本 + revise feedback。来源：`contract_versions`。

**实时事件流**
- 全部 / OpenClaw / 系统 / Claude（Gemini / Codex 占位）分流。来源：Event Bus。

### 5.3 右栏：CLI 过程流 + 一问一答

| 区域 | 显示 | 数据来源 | 交互 |
| --- | --- | --- | --- |
| 模型按钮 | Claude（可用）/ Gemini（占位）/ Codex（占位） | 静态 | **点 Claude → 进入对该 CLI 的一问一答** |
| 过程流 feed | 每条：模型名 + 时间 + 角色标签（规划/文献/复现…）+ 摘要 + 挂载产物文件 | `raw_log`（workflow 模式 transcript） | 点条目 → 看完整 transcript / 产物详情 |
| 输入框 | 给当前选中 CLI 发消息（consult 模式） | — | 发送 → headless 跑一轮 → 流式回显 |
| 产物列表 | 文件名 + 大小 + 时间 + 来源模型 | artifacts + raw_log | 点击查看 |

**「重要内容」如何提取**：右栏 feed 不展示原始 stream-json 全文，而是每轮 CLI 运行结束后，由后端提取一条结构化摘要项 `{ model, time, role, summary, artifact_refs }` 推送；点击才拉全量 transcript。这既满足「实时显示重要内容、点进去看详情」，又不把噪音怼到脸上。摘要项本身也是 raw_log 的一部分，不是结论。

### 5.4 实时更新机制

- `GET /projects/:id/stream`（SSE）：推送 `phase_changed` / `artifact_committed` / `gate_result` / `cli_chunk` / `consult_message` 事件。
- 面板订阅 SSE，增量更新；断线降级为轮询 `GET /projects/:id/state`。
- CLI stream-json 事件经 Event Bus 转成 `cli_chunk` 实时进右栏。

## 6. 要实现的指标

分两类：**验收指标**（v2 是否完成的硬标准）与 **面板运行指标**（界面上展示的实时数字）。

### 6.1 功能验收指标（达标才算 v2 完成）

| # | 指标 | 达标标准 |
| --- | --- | --- |
| V1 | 真实链路打通 | 至少 1 个 phase（建议 contract_draft）由真实 Claude Code(Haiku) 产出，过 contract gate，可 human approve |
| V2 | 结构化保证 | CLI workflow 模式产出 100% 经 schema/gate 校验；非法输出被拦截并可回退，绝不直接 commit |
| V3 | 两通道分离 | 面板任一「结论」均可溯源到 artifact id；transcript 一律标记为 raw_log，无法被当作结论引用 |
| V4 | 回退闭环 | 任一 gate fail 后流程不死锁：可回退到指定上游 phase 或从 blocked recover；有自动化测试覆盖 |
| V5 | 真 revise | revise 后产出内容实质不同的新版本草稿，变更历史 tab 可见 diff/feedback |
| V6 | 证据闭环 | `claimEvidenceGate` 生效；证据映射 tab 显示真实 claim→artifact 命中与缺口；缺口存在时禁止进 success summary |
| V7 | 一问一答可用 | 面板点 Claude 发消息能拿到 Haiku 真实回复，整轮存为 consult raw_log，且不推进 state |
| V8 | 实时可视 | phase 推进、artifact 落地、CLI 过程在面板上经 SSE 实时出现，无需手动刷新 |
| V9 | mock 回放仍稳 | 不接 CLI 时 `npm test` 与 `demo:fixture` 全绿 |
| V10 | 成本护栏生效 | 默认 Haiku；可配置 token/turn/超时上限；无 key 或超限时优雅降级到 mock，不崩 |

### 6.2 面板运行指标（界面实时展示）

| 指标 | 含义 | 来源 |
| --- | --- | --- |
| 总体进度 % | 已 pass 阶段 / 总阶段 | `phase_history` |
| 当前阶段已用时 / 预计剩余 | 阶段计时（预计可先用经验值或留空） | timestamps |
| gate 通过率 | pass / (pass+fail) | `phase_history` |
| artifact 数量 | 已 commit 结构化产物数 | evidence store |
| 证据覆盖率 | 已命中 claim / 总 claim | `claimEvidenceGate` |
| 本次会话 token / 估算成本 | Claude Code 用量累计 | stream-json usage 字段 |
| CLI 调用次数 / 失败次数 | workflow + consult 运行统计 | adapter 计数 |

**成本是一等公民指标**：面板必须显示「本会话累计 token / 估算花费」，并在接近配置上限时告警。这是「不浪费钱在调试上」的可视化抓手。

### 6.3 gate 最低质量约束（P1-5 具体化）

- 复现清单：`commands` 必须形如可执行命令（非空字符串 + 含可识别命令动词）；`expected_metrics` 至少有一项能对上 `contract.metrics[].name`。
- baseline：`selected.paper_id` 必须能在 `candidates` 或 paper cards 中找到对应项。
- idea review：每个 idea 六维评分齐全且为数值（v1 已做），新增 ranking 与 `recommended_idea_id` 一致性校验。
- 证据映射：每条 claim 的 `required_evidence` 项必须有对应 artifact 类型命中。

## 7. 成本与安全约束（硬性）

- **默认模型 `claude-haiku-4-5`。** Opus / Sonnet 仅在显式配置时使用，且面板/日志要警示。
- 单次 CLI 运行上限：`--max-turns`（建议 ≤ 20）、超时（建议 ≤ 120s，可配）、token 预算上限；超限即中止并标记 fail。
- 会话级累计预算：超过阈值暂停 CLI 调用，提示人工确认。
- 无 API key / Claude Code 不可用时：自动 fallback mock，流程不中断。
- 沙箱：CLI 限定在独立工作目录，受限 `--allowedTools`，默认不开放危险工具（不默认允许任意 Bash / 删除）。
- evidence store 不写 API key、不写完整敏感环境变量、不写未白名单 tool input（沿用 v1 `redactSecrets`）。

## 8. 里程碑

| 里程碑 | 交付物 | 验收 |
| --- | --- | --- |
| **M0 后端硬骨头** | P0-1 回退/recover、P0-2 真 revise、P0-3 gate 统一、P1-4 claimEvidenceGate、P1-5 质量门 | 新增状态机/gate 测试全绿；`demo:fixture` 仍跑通；证据映射有真实计算结果 |
| **M1 实时通道 + 新面板（mock 数据）** | Event Bus、SSE endpoint、三栏面板、证据映射/变更历史 tab、产物详情 | 纯 mock 下面板实时反映 phase/artifact/gate；点击 artifact 看详情 |
| **M2 ClaudeCodeAdapter workflow 模式** | headless 调用、Haiku 默认、forced schema、沙箱、成本护栏 | contract_draft 由真实 Haiku 产出并过 gate；非法输出被拦；超限/无 key 降级 mock |
| **M3 consult 一问一答** | consult endpoint、`--resume` 多轮、raw_log 记录、promote-to-artifact | 面板点 Claude 对话拿到真实回复；不推进 state；可手动 promote |
| **M4 联调 + 指标面板** | 运行指标、成本展示与告警、OpenClaw 触发联调、smoke log | §6.1 验收指标 V1–V10 全部达标 |

建议顺序严格 M0 → M4：先让状态机不死、证据闭环成立，再上实时面板，再接真实 CLI，最后补一问一答与指标。**不要先搭右栏对话壳子再补后端**，否则重蹈「看起来闭环、实则悬空」。

## 9. 测试要求

- 沿用 v1 全部测试（gateway / contract / engine transition / workflow fixture / ui），保持全绿。
- 新增：
  - 回退/recover 状态机测试（baseline fail → 回退；blocked → recover）。
  - 真 revise 内容变化测试。
  - `claimEvidenceGate` 命中与缺口测试。
  - `ClaudeCodeAdapter` 以**录制 fixture / stub 子进程**方式测试（不在 CI 真调 CLI）。
  - 成本护栏：超时 / 超 turn / 无 key 降级 mock 测试。
  - SSE 事件序列测试。
- CI 默认全 mock、不消耗任何模型额度。真实 Claude Code 调用仅本地 / 显式开关下进行。

## 10. 风险与对策

| 风险 | 对策 |
| --- | --- |
| 把对话「关键信息」当结论 → 护城河失效 | §2.1 两通道红线；面板结论只允许引用 artifact id |
| CLI 非确定 / 慢 / 失败拖垮 demo | mock 为回放底座；超时/降级；CI 不调真 CLI |
| 调试烧钱 | 默认 Haiku；token/turn/会话预算上限；面板实时成本告警 |
| CLI 文件/shell 自主权过大 | 沙箱目录 + 受限 allowedTools + 默认不放危险工具 |
| 前端跑在后端能力前 | 严格 M0→M4；证据映射/对话必须接真数据后才上 |
| state.json 并发覆盖 | orchestrator 内对同一 projectId 串行化（可与 M0 一起做，列为 P3 兜底） |

## 11. 给评审的话术

> v2 在 v1 的可约束骨架上做了三件事：让流程失败能回退、让每条结论都能追溯到证据、让真实模型（Claude Code）的工作过程实时可见且可随时插话。我们刻意只接了一个最稳的 CLI、默认最便宜的模型，先把「真实模型 + 机器校验契约 + 证据闭环 + 可回退」这条主链路证扎实，而不是先铺满界面。

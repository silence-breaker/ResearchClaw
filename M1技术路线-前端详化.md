# M1 技术路线（实时通道 + 新面板）— 前端交接文档

本文档给**接手 M1 的开发者**。读完应能独立搭起前端框架、对接后端、按里程碑推进，不需要再回头问架构。

前置：M0 已完成并合入（见 `M0成果总结.md`）。状态机、gate、证据闭环、`recover()` 都已就绪且有测试。

## 0. 一句话目标

```text
用真实后端数据（来自现有 mock-adapter 流程）驱动一个三栏研究控制台，
phase 推进 / artifact 落地 / gate 结果 / 证据映射 通过 SSE 实时反映到面板，
人类能在面板上 approve / revise / advance / recover，无需手动刷新。
本里程碑不接真实 CLI（那是 M2），不做人机一问一答对话（那是 M3）。
```

## 1. 范围

### 1.1 M1 必须做

- 后端：SSE 实时推送通道 + 事件总线；补 `POST /projects/:id/recover` 端点；用框架构建产物作为静态资源服务；dev 跨域。
- 前端：React + TypeScript 框架工程；三栏面板；项目列表；契约 / 阶段状态 / **证据映射** / 变更历史 四个 tab；当前阶段卡片 + 关键产物预览；artifact 详情查看；运行指标（进度% / gate 通过率 / 证据覆盖率 / artifact 数）。
- 交互：approve / revise / advance / recover 全部能从面板触发并实时看到结果。

### 1.2 M1 明确不做（占位即可）

- 右栏的模型按钮 **Claude / Gemini / Codex 一律 disabled 占位**，标注「M2/M3 接入」。
- 右栏「过程流 feed」M1 先展示已有的 `raw_log` 类 artifact（mock 流程产出的有限内容）；真实 CLI transcript 是 M2。
- 「点击 CLI 一问一答对话」输入框 M1 可渲染但 disabled；真实对话是 M3。
- 不接真实模型、不碰 CLI、不引入数据库。

## 2. 技术栈（推荐，可替换）

| 关注点 | 推荐 | 理由 |
| --- | --- | --- |
| 框架 | **React 18 + TypeScript** | 生态最大、招人最易、和后端 TS 类型可对齐 |
| 构建 | **Vite** | 启动快、dev proxy 简单、产物即静态资源 |
| 样式 | **Tailwind CSS**（深色主题） | 贴合概念图的 IDE 质感，迭代快 |
| 数据获取 | **TanStack Query** | 缓存、失效、重试开箱即用 |
| 实时 | **原生 `EventSource`（SSE）** | 单向推送足够，比 WebSocket 轻 |
| 轻量状态 | React Context 或 **Zustand** | 存当前 projectId、语言、SSE 连接态 |
| 路由 | **React Router** | 项目列表 ↔ 面板两级路由 |

> 如果团队更熟 Vue/Svelte，可整体替换框架层——本文档第 4 节（数据契约）和第 6 节（组件/数据映射）是框架无关的，照搬即可。**不要因为换框架就改后端数据契约。**

工程位置：`researchclaw/web/`（独立 Vite 应用）。dev 时 Vite（:5173）代理 API 到 :8787；build 产物由 `server.js` 作为静态资源托管（见 §3）。

## 3. 后端需要先做的改动（M1 的服务端部分）

现有 `server.js` 是手写 HTTP，路由清晰（见现有 approve/revise/advance 模式）。M1 加四样：

### 3.1 事件总线 `engine/events.js`

进程内 `EventEmitter`，按 `projectId` 广播。orchestrator 注入它（可选，保持向后兼容：不注入则不广播，所有现有测试不受影响）。

### 3.2 在每次状态写入后广播 `snapshot`

**最小且最稳的设计**：在 `orchestrator.finish()` 和 `finishManual()` 写完 state 后，emit 一个 `snapshot`（内容 = 完整 state.json）。

> 单一 `snapshot` 事件就能驱动整个面板：前端收到就整体替换 state，所有结构化视图（pipeline / 契约 / 证据映射 / 指标）都从 state 派生。活动 feed 也能从 `state.phase_history` + `state.signals` 派生。granular 事件（见 §4.3）是增强项，不是 M1 必需。

### 3.3 SSE 端点 `GET /projects/:id/stream`

```
Content-Type: text/event-stream
```
连接时先推一帧当前 `snapshot`，之后订阅该 projectId 的事件转发为 SSE；连接关闭时取消订阅；定时 `:heartbeat` 注释帧防超时。

### 3.4 补 `POST /projects/:id/recover`

body `{ to? }` → 调 `orchestrator.recover(id, { to })` → 返回 finishManual 结果。**为它补 HTTP 层测试**（M0 留下的尾巴）。

### 3.5 静态资源 + dev 跨域

- build 产物（`web/dist`）由 server 托管：非 API 路径回退到 `index.html`（SPA fallback）。
- dev：给 API 响应加 `Access-Control-Allow-Origin`（dev 用 Vite proxy 可免，二选一，proxy 更干净）。

> 现有 `ui.js`（手写 HTML 面板）M1 期间保留可用，新前端就绪后再切换默认入口。不要急着删 `ui.js`，它的 `ui.test.js` 还在测试套件里。

## 4. 数据契约（前后端之间的硬接口）

以下类型直接来自 M0 后实际代码，前端按此定义 TS 类型。

### 4.1 核心类型

```ts
type ResearchPhase =
  | "idle" | "intake" | "contract_draft" | "contract_review"
  | "literature_scouting" | "baseline_selection" | "baseline_reproduction_checklist"
  | "idea_generation" | "idea_review" | "summary" | "blocked";

interface ProjectState {
  state_version: number;
  project_id: string;
  phase: ResearchPhase;
  created_at: string;
  updated_at: string;
  current: {
    research_direction?: string;
    intake_artifact_ref?: string;
    contract_artifact_ref?: string;
    contract_artifact_id?: string;
    literature_artifact_ref?: string;
    baseline_artifact_ref?: string;
    checklist_artifact_ref?: string;
    idea_artifact_ref?: string;
    review_artifact_ref?: string;
    summary_artifact_ref?: string;
    raw_log_artifact_refs?: string[];
  };
  pending_human_actions: PendingAction[];
  phase_history: PhaseHistoryEntry[];
  signals: SignalRef[];
  contract_versions: { version: number; artifact_ref: string; status: string }[];
  latest_signal_id?: string;
  block?: { failed_phase: ResearchPhase; retreat_to?: ResearchPhase; errors: string[] };
}

interface PhaseHistoryEntry {
  phase: ResearchPhase;
  artifact_refs: string[];
  gate_result: "pass" | "fail" | "manual";
  timestamp: string;
}

interface SignalRef {
  id: string; intent: string; event: string; routeKey: string;
  timestamp: string; rawPayloadRef: string;
}

// pending_human_actions 的几种形态（按 type 区分）：
type PendingAction =
  | { type: "provide_research_direction"; target: string; label: string }
  | { type: "approve_or_revise"; target: "contract"; artifact_id: string; artifact_ref: string; label: string }
  | { type: "run_phase"; phase: ResearchPhase; label: string }
  | { type: "revise_required"; phase: ResearchPhase; retreat_to?: ResearchPhase; errors: string[]; label: string }
  | { type: "next_human_action"; description: string }
  | { type: string; [k: string]: unknown }; // 兜底（hook 触发的杂项）

interface Artifact<T = unknown> {
  artifact_id: string;
  project_id: string;
  phase: ResearchPhase;
  type: "contract" | "paper_cards" | "baseline_decision" | "reproduction_checklist"
      | "idea_cards" | "idea_review_report" | "summary" | "raw_log";
  created_at: string;
  producer: { workflow: string; adapter: "mock" | "claude" | "gemini" | "codex" | "manual" };
  input_refs: string[];
  evidence_refs: string[];
  content: T;
  status: "draft" | "accepted" | "rejected" | "superseded";
}

// summary artifact 的 content.evidence_index —— 证据映射 tab 的数据源
interface EvidenceIndexEntry {
  claim_id: string;
  claim: string;
  satisfied: { evidence: string; artifact_ref: string }[];
  pending: { evidence: string; reason: string }[];
}
```

### 4.2 HTTP 端点

| 方法 | 路径 | 请求体 | 响应 |
| --- | --- | --- | --- |
| GET | `/projects` | — | `{ ok, projects: { project_id, phase, updated_at, pending_human_actions }[] }` |
| GET | `/projects/:id/state` | — | `{ ok, state: ProjectState }`（**完整 state**） |
| GET | `/projects/:id/artifact?ref=<ref>` | — | `{ ok, artifact: Artifact }` |
| POST | `/projects/:id/start` | `{ research_direction }` | `{ ok, project_id, phase, actions, panel_url, state }`（state 为精简摘要） |
| POST | `/projects/:id/advance` | — | 同上 |
| POST | `/projects/:id/approve` | `{ target:"contract", artifact_id, approved_by, note? }` | 同上 |
| POST | `/projects/:id/revise` | `{ target:"contract", feedback, artifact_id? }` | 同上 |
| POST | `/projects/:id/recover` | `{ to? }` | 同上（**M1 新增**） |
| GET | `/projects/:id/stream` | — | SSE（**M1 新增**） |

> 注意：action 端点返回的 `state` 是 `publicStateSummary`（精简：project_id/phase/current/pending_human_actions/latest_signal_id/updated_at）。前端**不要**只依赖它渲染全部视图——结构化视图统一从 `GET /state` 的完整 state（或 SSE 的 `snapshot`）取数。action 响应只用来判断成功 + 读 `actions`。

### 4.3 SSE 事件

M1 必需：

| event | data | 用途 |
| --- | --- | --- |
| `snapshot` | 完整 `ProjectState` | 连接时 + 每次状态变更后，前端整体替换 state |
| `:heartbeat`（注释帧） | — | 保活 |

可选增强（驱动 feed 动画，能从 snapshot 派生，非必需）：

| event | data |
| --- | --- |
| `phase_changed` | `{ from, to }` |
| `artifact_committed` | `{ phase, artifact_id, ref, type }` |
| `gate_result` | `{ phase, result, errors? }` |
| `blocked` | `{ failed_phase, retreat_to, errors }` |
| `recovered` | `{ to }` |

## 5. 前端目录结构（建议）

```text
researchclaw/web/
  index.html
  vite.config.ts            # dev proxy /projects,/openclaw -> :8787
  src/
    main.tsx
    api/
      client.ts             # fetch 封装 + 端点函数
      types.ts              # §4 的 TS 类型
      useProjectStream.ts   # EventSource 订阅 -> state（断线回退轮询 /state）
    routes/
      ProjectList.tsx        # GET /projects
      Panel.tsx              # /panel/:projectId 三栏容器
    components/
      left/   ProjectNav, CurrentResearch, HistoryList, RecentEvents, GatewayStatus
      center/ PipelineProgress, CurrentPhaseCard, ArtifactPreview,
              ContractTab, PhaseStatusTab, EvidenceMapTab, ChangeHistoryTab, ActivityFeed
      right/  ModelLane(占位), ProcessFeed(raw_log), ConsultInput(disabled), ArtifactList
      shared/ ArtifactDetailDrawer, MetricBadge, PhaseNode, ActionButton
    store/    session.ts (zustand: projectId, lang)
    styles/   tailwind.css
```

## 6. 三栏布局与逐组件数据映射

**视觉权重原则**：契约 / 证据映射 / pipeline 进度居中心 C 位（项目护城河）；右栏 CLI 区是「过程透明度」，M1 还基本是占位，别让它喧宾夺主。

### 6.1 左栏

| 组件 | 显示 | 数据源 | 交互 |
| --- | --- | --- | --- |
| ProjectNav | 概览 / 研究工作流 /（文献库·实验管理·记忆库 灰显占位）/ 设置 | 静态 | — |
| CurrentResearch | 当前项目标题、phase、已用时 | state.phase + created_at | 点击进面板 |
| HistoryList | 项目列表 + 状态（完成/`blocked`=需修订） | `GET /projects` | 切换；blocked 项显「可回退到 X」并提供 recover |
| GatewayStatus | 连接态 | server 健康检查（可先写死「正常」） | — |
| RecentEvents | 最近 signals | state.signals 尾部 | — |

### 6.2 中栏（C 位）

| 组件 | 显示 | 数据源 | 交互 |
| --- | --- | --- | --- |
| PipelineProgress | 横向 phase 节点 + 总体进度% | state.phase + phase_history | 节点状态：pass/进行中/待运行/**blocked 需修订** |
| CurrentPhaseCard | 当前 phase 名 + pending action + 关键产物预览 | state + 当前 phase artifact content | 「查看完整产物」开抽屉；若 pending=run_phase 显「推进」按钮 → advance |
| ContractTab | 研究问题/假设/成功标准/失败信号/指标/数据划分 | contract artifact content | approve / revise 按钮 |
| PhaseStatusTab | 每个 phase 的 gate 结果 + artifact ref | phase_history | 点 ref 开抽屉 |
| **EvidenceMapTab** | 逐条 claim → satisfied / pending，缺口高亮 | summary artifact 的 `content.evidence_index`（summary 前可调一个只读预览接口或显示「待 summary」） | 点 artifact_ref 开抽屉。**必须填真数据，不许写死** |
| ChangeHistoryTab | contract 各版本 + revise feedback | state.contract_versions（+ 读各版本 artifact 的 human_notes） | 版本对比 |
| ActivityFeed | 全部/系统/Claude(占位)… | phase_history + signals（或 SSE 增强事件） | 分流过滤 |

**approve/revise/recover 交互**：按钮 → 调对应 POST → 不手动改本地 state，等 SSE `snapshot` 回来刷新（乐观更新可选）。

### 6.3 右栏（M1 多为占位）

| 组件 | M1 行为 |
| --- | --- |
| ModelLane（Claude/Gemini/Codex 按钮） | **disabled**，tooltip「M2/M3 接入」 |
| ProcessFeed | 渲染 `raw_log` 类 artifact 列表（mock 流程产出的有限内容）；点击开抽屉 |
| ConsultInput | 渲染但 disabled，placeholder「M3 开放对话」 |
| ArtifactList | 全 artifact 文件名 + 大小 + 来源 adapter + 时间 | 点击开抽屉 |

## 7. 运行指标（面板顶部，M1 可全部从 state 派生）

| 指标 | 计算 |
| --- | --- |
| 总体进度% | `phase_history.filter(pass).length / 总 phase 数` |
| gate 通过率 | `pass / (pass+fail)` |
| 证据覆盖率 | summary 后：`Σsatisfied / Σ(satisfied+pending)`（来自 evidence_index） |
| artifact 数 | 已 commit artifact 计数 |

> 成本/token 指标是 M2（接真实 CLI 才有用量）——M1 先不显示或显「—」。

## 8. 实时策略

- 进面板：`GET /state` 拿首帧 → 同时 `new EventSource('/projects/:id/stream')` 订阅 `snapshot`。
- 收到 `snapshot`：整体替换 state（React Query `setQueryData` 或 store set）。
- 断线：`EventSource` 自动重连；额外用 TanStack Query 每 5s 轮询 `/state` 兜底。
- 所有 mutation（approve/revise/advance/recover）成功后**不本地拼装 state**，统一等 `snapshot`。

## 9. M1 内部里程碑

| 步 | 交付 | 验收 |
| --- | --- | --- |
| M1.0 | Vite+React+TS+Tailwind 脚手架 + dev proxy；server SPA 托管 | `web` 能起、能打到 :8787 |
| M1.1 | 后端事件总线 + `snapshot` 广播 + SSE 端点 + recover 端点（带测试） | curl SSE 能在 advance 后收到 snapshot；`recover` 端点有测试 |
| M1.2 | 项目列表 + 面板骨架 + 中栏 pipeline + 契约 tab（只读） | 真实 state 驱动，phase/契约正确显示 |
| M1.3 | approve/revise/advance/recover 交互 + SSE 实时刷新 | 面板操作后无需刷新即更新；blocked 能 recover |
| M1.4 | 证据映射 tab + 变更历史 tab + artifact 详情抽屉 + 指标 | 证据映射显真实 satisfied/pending；指标正确 |
| M1.5 | 右栏占位（模型按钮 disabled、ProcessFeed、ArtifactList） | 占位明确标注，不误导 |

## 10. M1 验收标准

- 纯 mock 流程下，面板用真实后端数据反映 phase / 契约 / 证据映射 / 指标。
- approve / revise / advance / **recover** 可从面板触发，结果经 SSE 实时出现，无需手动刷新。
- 证据映射 tab 显示真实 `evidence_index`（satisfied/pending），**不允许写死**。
- 点击任一 artifact 能看结构化详情；任一「结论」都能溯源到 artifact ref。
- 右栏模型/对话明确占位，不假装已接入。
- 现有 `npm test`（含 ui.test.js）保持全绿；新增 recover 端点 / SSE 有测试。
- CI 仍全 mock、不消耗模型额度。

## 11. 给下一个人的红线（务必遵守）

1. **结论只来自 artifact。** 面板任何「结论」必须能点到对应 artifact ref；证据映射必须用 `claimEvidenceGate` 的真实输出，不许为了好看写死。
2. **不要让前端跑在后端能力前。** 右栏 CLI 对话是 M3，M1 就老老实实 disabled 占位。先把契约+证据+pipeline 这条真实链路在界面上立住。
3. **不改数据契约去迁就 UI。** 需要新字段先在后端加并补测试，engine 仍是唯一状态源。
4. **engine 边界不破**：前端只调 HTTP，不直接操作 state；workflow/adapter/gateway 的职责边界（见《第二版技术路线指南》§2）继续守住。
5. **mock 永远能跑**：任何改动后 `demo:fixture` 和 `npm test` 必须仍绿。

## 12. 对接已有代码的提示

- 状态机入口：`engine/orchestrator.js`（`advance/approve/revise/recover/startFromText`）。
- 证据映射算法：`engine/gates.js` 的 `claimEvidenceGate`，输出结构见 §4.1 `EvidenceIndexEntry`。
- artifact 读写：`evidence/store.js`（`readArtifact(projectId, ref)` / `appendArtifact`）。
- 现有 HTTP 路由模式：`server.js`（照 approve/revise 的写法加 recover/stream）。
- 现有手写面板：`ui.js` + `ui.test.js`（保留，新前端就绪后再切默认入口）。

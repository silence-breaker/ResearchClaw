# M0 成果总结（后端硬骨头）

对应《第二版技术路线指南》里程碑 M0。承接《第一版锐评》的 P0/P1。全程严格 TDD（先写失败测试 → 看红 → 最小实现 → 转绿）。

## 一句话结论

第一版状态机的两个致命缺陷——**失败即死锁**和**证据闭环悬空**——已经修好。流程现在能回退、能恢复，结论只能引用真实证据，summary 不再替没做的实验背书。这是 v2 接 CLI 和新面板之前必须先立稳的地基。

## 测试与验证

- `npm test`：**34/34 通过**（原 15 + 新增 19）。
- `npm run demo:fixture`：离线全链路闭环正常，跑到 summary。
- 新增测试文件 `researchclaw/tests/gates.test.js`（15 个纯 gate 单测）；`engine.test.js` 新增 4 个状态机测试。

## 交付明细

| 锐评项 | 内容 | 关键文件 |
| --- | --- | --- |
| **P0-3** | `ideaGate` 从 orchestrator inline 抽进 `gates.js`，并强化：每个 idea 必须有 `id` / `title` / `baseline_compatibility` / 非空 `evidence_refs` | `engine/gates.js`、`engine/orchestrator.js` |
| **P1-5** | 三个 gate 从「非空」升级到「最低质量」：<br>· `baselineGate`：`selected.paper_id` 必须命中候选或 paper cards<br>· `reproductionChecklistGate`：`commands` 须像可执行命令、`expected_metrics` 须对上 `contract.metrics[].name`<br>· `reviewGate`：`ranking` 覆盖所有 idea，`recommended_idea_id` 不得指向 `kill` | `engine/gates.js` |
| **P0-2** | `revise` 真重跑：不再是纯本地 stamp，改为经 `contractDraft` workflow + adapter 重新产出新版本草稿，feedback 真正进入新草稿并过 gate（`producer.workflow === "contractDraft"`） | `engine/orchestrator.js`、`workflows/contractDraft.js`、`adapters/mock.js` |
| **P0-1** | `blocked` 可恢复：gate fail 仍进 `blocked`，但携带 `block.retreat_to` 回退目标；新增 `orchestrator.recover()` 能回到上游可重跑 phase。`blocked` 不再是死路 | `engine/orchestrator.js` |
| **P1-4** | 证据闭环落地：新增 `claimEvidenceGate`，按 `evidence→phase` 注册表逐条把 claim 证据分类为 `satisfied` / `pending`；已完成 phase 欠证据 = 真缺口 = block，未来/超范围 phase = pending = 放行。summary 的 `evidence_index` 由真实计算生成 | `engine/gates.js`、`engine/orchestrator.js`、`workflows/summary.js`、`adapters/mock.js` |

## 最能说明价值的验证

demo 跑完后，summary 的 `evidence_index` 不再是写死的「已被 artifact 支撑」，而是诚实显示：

```json
{
  "claim_id": "C1",
  "claim": "The proposed reranker improves domain-specific retrieval quality.",
  "satisfied": [],
  "pending": [
    { "evidence": "baseline result", "reason": "out of demo scope (experiment)" },
    { "evidence": "same-split comparison", "reason": "out of demo scope (experiment)" },
    { "evidence": "main metric table", "reason": "out of demo scope (experiment)" }
  ]
}
```

系统现在**拒绝替还没做实验的结论背书**——这正是《科研助手架构批判分析》要的「证据闭环 / 不许看到结果再补故事」。

## 新增的引擎能力（M1 会用到的接口）

- `orchestrator.recover(projectId, { to? })`：从 `blocked` 回到 `to` 或 `state.block.retreat_to`，并把该 phase 标为可重跑。
- `claimEvidenceGate(contract, { availableArtifacts, completedPhases })`：返回 `{ ok, errors, evidence_index }`，`evidence_index` 即面板「证据映射」tab 的数据源。
- `state.block`：`{ failed_phase, retreat_to, errors }`，面板「需修订/可回退」状态的数据源。

## 留给 M1 的小尾巴

- `recover()` 的引擎方法 + 测试已就绪，但 **HTTP `POST /projects/:id/recover` 端点未做**——`server.js` 的 HTTP 层目前无测试夹具，留到 M1 建面板/SSE 时连同测试一起补。

## 边界守住情况

- gateway 不判阶段、workflow 只产 artifact、engine 不直接调模型——M0 全程未破坏。
- mock 仍是唯一测试/回放底座，未引入任何真实模型调用。

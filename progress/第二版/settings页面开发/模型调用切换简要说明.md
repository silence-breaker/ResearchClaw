# 设置页面模型切换UI需求

    research contract
  -> literature scouting
  -> baseline selection
  -> baseline reproduction checklist
  -> idea generation
  -> idea review
  -> summary + evidence memory

这每一个阶段都能有对应的模型切换（限制开启数量），没有多agent环节的就只能三选一

除了选择可不可以开启，还要选择模型的类型（实质就是底层开了claude code headless之后/mdoel自动选择）

基于cc-switch，故API等不需要重复配置

# ResearchClaw Settings 界面开发交接文档

> 交接时间: 2026-06-04
> 当前分支: `WHC` ｜ 最新提交: `ce9c417`（SecuritySettings + accessibility improvements + bug fixes）
> 启动方式见同目录《ResearchClaw启动指南.md》

---

## 1. 一句话现状

Settings 界面全部 **8 个一级分类 UI 已落地**（通用/外观/模型/通知/安全/个人资料与账户/辅助功能/系统），`typecheck` 绿色 + **62/62 测试通过**。下一步是 **M2** 模型配置后端接口对接 与 **M3** 安全认证/通知真实 API 集成。

---

## 2. 已完成里程碑

### Step 1 — 清理与基线
- 删除 `oh-my-claude-code/`（~20MB, 1470 文件），避免污染模型上下文。
- `WHC` 已包含 `WJ` 最新代码（M1.0~M1.3，49/49 后端 + 26/26 前端测试）。

### Step 2~5 — 骨架与状态管理
- 新增 `/settings` 路由（`router.tsx`, `basename: "/app"`）。
- 左栏「设置」从 placeholder 变为可点击 `<Link>`，支持 `noProject` 模式高亮。
- Zustand settings store + `persist` middleware（localStorage），带 preview 机制（draft → `setPreview` → SettingsEffect 合并 → 实时视觉反馈）。
- `lib/settings.ts`（TDD）：defaults / validation / migration，29 字段。`settings.test.ts` 11/11。
- `components/SettingsEffect.tsx`：CSS 变量实时应用（`--rc-*`），支持 preview 合并。

### Step 6~7 — 8 分类重构 + 辅助功能骨架
- 设置页重排为 8 个一级分类：个人资料与账户 / 通用 / 外观 / 模型 / 通知 / 安全 / 辅助功能 / 系统。
- Edge 风格分层级卡片布局：home 视图 8 张分类卡片（hover lift），detail 视图面包屑 + 设置表单。
- 视觉层级规范：`Section` 标题（`text-base font-bold border-l-4 border-accent`）vs `Field` 标签（`text-sm font-medium`）。
- 辅助功能 4 字段：减少动画 / 高对比度 / 屏幕阅读器优化 / 焦点指示器。

### Step 8 — 个人资料与账户
- `components/ProfileSettings.tsx`：Avatar 上传（base64, 2MB 限制）、nickname 编辑、账户切换下拉、logout。
- `lib/user.ts` + `user.test.ts`（TDD, 10/10）：UserProfile 类型与工具函数。
- `stores/user.ts`：User Zustand store（多账户、头像、编辑）。

### Step 9~10 — GeneralSettings 丰富化 + 布局打磨
- GeneralSettings：语言 / 时区（7 选项）/ 日期格式 / 导出路径 / 自动保存间隔 / 启动页 / 删除确认 / 音量滑块（0~100%）。
- AppearanceSettings overhaul：主题模式 + 夜间模式 / 亮度（50~150%）/ 字体大小 / 密度 / 统一字体族 / 强调色选择器 / 背景/聊天/代码颜色选择器。
- **Draft Preview 机制**：所有外观字段使用 draft local state + `setPreview` → SettingsEffect 合并 → 确认保存/取消回滚/卸载清除。

### Step 11 — ModelSettings（M2 占位）
- QQ-list 风格 UI：5 个 demo 模型（Claude/Gemini/GPT/Llama）。
- 功能：重命名、token 限制、强制停止/恢复、状态监控。
- `lib/model.ts` + `model.test.ts`（TDD, 7/7）：模型类型与工具。
- **M2 API stubs 已预留**，见 `lib/model.ts` 注释。

### Step 12 — NotificationSettings
- 消息/声音/弹窗/邮件 总开关。
- 阶段完成/错误/提及/@/系统公告 分项开关。
- 通知频率（即时/批量/日报）、免打扰 + 安静时段（起止时间）。

### Step 13 — SecuritySettings
- **密码修改**：当前密码 / 新密码 / 确认密码，密码强度指示器。
- **API 密钥管理**：add / reveal / copy / delete，masked 显示（`maskKey`）。
- **隐私管理**：数据保留天数、活动可见性。
- **2FA 切换**：toggle UI（占位，等 M3 后端）。
- `lib/security.ts` + `security.test.ts`（TDD, 8/8）+ `stores/security.ts`（Zustand store）。

### Step 14 — 辅助功能增强 + Bug 修复
- **reduceTransparency**：关闭 backdrop-blur 和毛玻璃效果，纯 CSS class toggle，反而提升 GPU 性能。
- **lineHeight**：行高调节（1.2~2.0，步进 0.1），CSS 变量 `--rc-line-height`。
- **Bug 修复 1**：`isValidPassword` 要求至少 1 个大写字母（与测试对齐）。
- **Bug 修复 2**：localStorage 迁移 — Zustand persist version `1→2`，确保老用户自动获得新字段默认值（原 `lineHeight` undefined 导致 `toFixed` crash）。
- **清理**：移除未使用的 `PlaceholderSettings` 组件。

---

## 3. 前端文件清单（`researchclaw/web/src/`）

```
routes/
  SettingsPage.tsx              主页面：8 分类路由 + 共享 UI 原语（Section/Field/ToggleField/SaveBar）
components/
  SettingsEffect.tsx            CSS 变量实时应用（settings + preview 合并）
  ProfileSettings.tsx           个人资料：头像/昵称/账户切换/logout
  ModelSettings.tsx             模型配置：QQ-list UI + demo data
  NotificationSettings.tsx      通知设置：开关矩阵 + 频率 + 免打扰
  SecuritySettings.tsx          安全设置：密码/API 密钥/隐私/2FA
stores/
  settings.ts                   Zustand settings store + persist + preview 机制
  user.ts                       User store（多账户/头像/编辑）
  security.ts                   Security store（密码/API 密钥/隐私/2FA）
lib/
  settings.ts(+test)            Settings 类型/默认值/校验/迁移                  11 测试
  user.ts(+test)                UserProfile 类型与工具                         10 测试
  model.ts(+test)               模型配置类型与工具（M2 stubs）                  7 测试
  security.ts(+test)            安全类型与工具（密码/API 密钥/掩码）            8 测试
styles/
  tailwind.css                  rc-* accessibility class + --rc-* CSS 变量
```

测试合计：**web vitest 62/62**，**后端 node --test 49/49**。

---

## 4. 必须守住的红线（WJ 红线 + 本项目特有）

| 红线 | 本项目遵守情况 |
|------|--------------|
| 结论只来自 artifact | ✅ 设置页不涉及结论 |
| 不让前端跑在后端能力前 | ✅ Security/模型/通知均诚实占位，不造假数据 |
| 不改数据契约迁就 UI | ✅ 设置类型为前端本地类型，不污染后端契约 |
| mock 永远能跑 | ✅ 零模型调用，CI 全 mock |
| TDD 优先 | ✅ 每个 lib 文件配 .test.ts |
| TS strict | ✅ `npm run typecheck` 零报错 |
| Tailwind panel/accent 色系 | ✅ 不使用硬编码色值 |
| 无 fake data 骗 backend-dependent 功能 | ✅ 占位组件标注 TODO(M2/M3)，disabled 或 mock 数据明确可识别 |

---

## 5. 已知限制 / 坑（交接重点）

- **Zustand persist 版本迁移坑**：新增 settings 字段时，必须**同时 bump persist version**（如 `1→2`），否则老用户 localStorage 里的旧数据缺少新字段，会导致组件读取 `undefined` 而崩溃。`stores/settings.ts` 的 `migrate` 函数会调用 `migrateSettings()` 填充默认值，但只有当 version 不匹配时才会触发。
- **SecuritySettings 敏感数据仅前端**：密码/API 密钥目前仅在前端 masked 展示，真实加密/哈希等 M3 后端接口。当前 store 用 localStorage 持久化，**不要**在本地存真实生产密钥。
- **ModelSettings 是占位 UI**：5 个 demo 模型为假数据，force stop/resume 仅切换前端状态，不调用后端。M2 需替换为真实模型管理 API。
- **light 主题未实现**：`tailwind.config.js` 目前 dark-only，theme 选项中的 `light`/`system` 骨架存在但样式未补齐。如需 light，需全面扩展颜色系统并回归测试。
- **NotificationSettings 无后端**：所有通知开关和频率仅前端本地状态，M3 需对接真实通知服务。

---

## 6. 预留的开发接口（TODO 清单）

### M2 — 模型配置后端对接
- `lib/model.ts`：`// TODO(M2): Replace with real model management API`
- `stores/settings.ts`：模型相关字段（`defaultModel`, `maxTokens` 等）目前为前端本地状态，M2 需扩展 schema 并从后端同步。
- `components/ModelSettings.tsx`：force stop/resume 需调用真实模型状态 API。

### M3 — 安全/认证/通知后端对接
- `lib/security.ts`：`// TODO(M3): Replace with real auth API integration`
- `stores/security.ts`：所有 action 标注 `// TODO(M3): API integration`
  - `changePassword` → POST `/auth/password`
  - `addApiKey` / `removeApiKey` → POST/DELETE `/auth/api-keys`
  - `toggleTwoFactor` → POST `/auth/2fa`
  - `updatePrivacy` → POST `/auth/privacy`
- `components/NotificationSettings.tsx`：通知偏好需同步到后端用户配置。

### 可选优化
- **light 主题**：扩展 `tailwind.config.js` light 色系，或切为 CSS 变量驱动动态主题。
- **更多辅助功能**：色盲滤镜（CSS filter）、大号光标、字间距调节、专注模式（distraction-free）。
- **键盘快捷键**：`?` 呼出快捷键浮层（类似 GitHub）。

---

## 7. 下一步推荐顺序

| 优先级 | 内容 | 依赖 |
|--------|------|------|
| P0 | M2 模型配置后端接口（`GET/POST /settings` 或 `/models`） | 后端新增接口 |
| P1 | M3 Security 后端对接（密码修改/API 密钥/2FA） | 后端 auth 系统 |
| P1 | M3 Notification 后端对接（通知偏好持久化） | 后端用户配置 |
| P2 | light 主题样式补齐 | 设计决策 |
| P2 | 代码块高对比度主题 | 纯前端 |
| P3 | 更多辅助功能（色盲滤镜/大号光标等） | 纯前端 |

---

## 8. 关键参考文档

- `workspace/docs/progress/settings-plan.md` — Settings 开发总体规划
- `workspace/docs/progress/settings-dev.md` — 逐日开发进度与文件清单
- 《M1技术路线-前端详化.md》— WJ 前端架构、实时策略、红线
- `researchclaw/web/README.md` — 前端 run 说明

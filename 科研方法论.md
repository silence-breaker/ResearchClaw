# AI 科研方法论

> 来源图片：`科研方法论.jpg`  
> 原图标题：AI 科研不完全指北：Top10 PhD 烧了上万刀的真实经验。Context Is All You Need  
> 作者：NeverMore / Jingxuan  
> 说明：本文是对图片内容的结构化提取与整理，不是逐字转写。原图中有一小段被截图选中浮层遮挡，相关处已按上下文概括。

## 核心观点

**Context Is All You Need。**

AI 做科研的关键，不是让 AI 一键生成一篇“看起来像论文”的东西，而是把科研过程中最难、最脏、最痛苦的部分系统化：文献调研、确定 baseline、复现 baseline、提出 idea、实现 idea、跑实验、分析结果、写论文。

真正重要的是管理好每一个 session 的上下文。模型做不好一件事，不一定是模型没有能力，而可能是上下文已经被污染、带偏或塞进了过多无关信息。后续 workflow 的设计，本质上都是在控制上下文。

## 作者背景与使用模型

作者自述是 Top10 CS PhD 在读，发过 CVPR、ICCV、ICML、NeurIPS 等顶会，也担任过主流顶会审稿人，因此能判断 AI 产出是否具有基本科研质量。

图中提到作者主要使用：

- Claude Opus 4.6
- GPT-5.4 xHigh
- Gemini 3.1 Pro

作者强烈不建议用低智能模型做科研探索，因为这很容易浪费生命。

作者还提到自己订阅过 Claude Max 20x、ChatGPT Pro、多个 GPT Plus、Gemini 家庭组 Ultra、Super Grok 等。目的不是炫富，而是说明这套 workflow 是长期反复迭代、真实跑通后的经验。

## 不要迷信现成“自动化科研框架”

图中提到作者尝试过：

- Paper2Code
- Sakana AI 的 The AI Scientist
- Oh My Research
- Hermes research skill
- 以及各种市面上的“自动化科研框架”

结论是：这些通用框架对真正产出一篇有价值的工作帮助很小。很多看起来高级的功能，如 AI 相互 review、多模型投票、多轮 loop 改论文等，在作者看来大多是伪需求。

如果你愿意自己用 Claude 花几个小时迭代，很可能比直接 clone 别人的 skill 或现成 agent 系统高效得多。

## AI 真正参与科研的底线

作者认为，AI 真正参与科研至少要守住以下底线：

1. **不允许任何形式的造假。**  
   所有结论必须有明确出处；提出的 idea 与最终实现的 method 必须完全对应；实验结果必须来自真实代码训练，任何人按照论文方法描述都能复现代码并跑出一致结果。

2. **不能只生成“更精致的学术垃圾”。**  
   如果结论不能保证真实性，那么从根上就走不通。AI 生成的东西如果只是更漂亮、更像论文，但污染性更强，那不是科研成果。

3. **workflow 的目的不是让 AI 写得更好看。**  
   核心目标是严格保证：idea 被验证过、实验真实跑出来、论文结论可复现。

4. **科研不能先看到结果再编故事。**  
   这是作者反复强调的禁忌。实验之前必须写清楚假设、成功标准、失败信号、消融预期、指标和数据划分。

## 总体流程

一篇深度学习论文从零到投稿，大致流程是：

1. 文献调研
2. 确定 baseline
3. 复现 baseline
4. 提出 idea
5. 实现 idea
6. 跑实验
7. 分析结果
8. 写论文

这个流程不是一条直线。从“提出 idea”到“分析结果”会反复循环：idea 不 work 就回去重想，结果有苗头但方法有问题就回去重改。论文定稿之前，这个循环通常会重复很多轮。

作者引用的一个判断是：一个 idea 直接拿到一个好实验结果并不一定是最好的科研；真正有价值的是探索过程，以及对结果的分析。真正经过探索得到的结果才属于你。

## 多模型协作：把模型当 ensemble

作者全程使用多个模型协作，让 Claude、GPT、Gemini 交叉 review。它们的关系类似 ensemble。

这样做的原因：

- 不同模型的知识覆盖范围不同。
- 不同模型的思考模式不同。
- 不同模型的幻觉倾向不同。
- 让模型互相 review，可以降低单模型盲区。

图中给出的体感分工：

- Claude：适合作为主 architect，负责组织、判断、整合流程。
- Gemini：搜索能力和知识量很强，适合做发散式文献搜索和 idea 生成。
- Codex：在上下文明确、隔离良好的情况下，适合做严格 review、代码实现与判断。
- GPT：可参与交叉 review，但具体分工在图中没有展开太多。

关键不是让模型无限互相回复，而是一定要有一个角色做最终决策。

## 文献调研

文献调研是最适合交给 agent 的环节之一。只要接了正确的工具，agent 的窗口知识量和阅读速度会远超人类，它能在很短时间里扫过大量论文，覆盖面远超过人一篇篇翻。

但问题是：怎样让它搜到真正相关、真正值得展开讨论的论文。

### 不理想的路线

作者早期尝试过几种路线：

- 接 MCP，连接 arXiv、Hugging Face、Web Search。
- 认为知识源越多、覆盖面越大越好。
- 接 Zotero，让模型优先从自己读过的论文里找。

实际效果并不理想。

原因：

- 普通搜索很难找到真正有价值的相关文章。
- Zotero 只能代表你读过的论文，不足以支撑新 idea 来源。
- 搜到什么、不该搜什么，应该由研究问题本身决定，而不是由阅读历史决定。

### 更有效的路线

后来作者把文献调研单独交给一个 scout agent，底层使用 Gemini CLI。

原因是：

- Gemini 的搜索能力强。
- Gemini 可以自然把 Claude 分派的任务转成结构化搜索请求。
- Gemini 会把结构化需求拆成多角度搜索。
- 后续 review idea、选 baseline、判断论文会议级别、判断是否有开源代码，都可以依赖这个搜索结果。

作者还在搜索流程里加了一个约束：让 Gemini 重点关注中高水平顶会，以及附带开源代码的工作。

### 文献进入上下文的方式

把整篇 PDF 直接丢给模型并不理想。模型经常不会从头读到尾，而是略读后回答问题。只有在追问时，它才会回去读漏掉的部分。

更好的做法：

- 能拿到 LaTeX 源文件或 Markdown，就直接读源文件。
- 不走 PDF。
- arXiv 上很多论文可以拿到源文件。
- Markdown 的好处是可以按需读：需要摘要就读摘要，需要方法就读方法章节。
- 在调研阶段，通常只需要摘要和 introduction，用来判断论文是否与当前方向相关，不必过早进入实验细节。

### 文献调研真正的价值

很多人以为文献调研不重要，让 AI 自己想 idea 就好，这是很大的误区。

文献调研的本质，是给大模型构建一个领域专家级上下文。拥有深耕方向的专家上下文，和拥有一个泛泛而谈的通用上下文，想出来的 idea 质量差距巨大。

如果调研阶段搜到的是一堆不相关论文，后面基于这些内容想出来的 idea 就很难推进。

## 确定 Baseline

图中强调：在开始讨论 idea 之前，一定要先确定 setting 和 baseline。

Paper2Code 曾尝试在没有 baseline 的情况下直接从论文生成代码，效果不理想。这也符合真实科研现状：极少数科研是从零写一整套全新代码，绝大多数工作都是基于某个 baseline 展开的。

确定 baseline 的原因：

- 让后续实验有可靠锚点。
- 判断 idea 有没有提升，必须放在同一个框架下。
- 论文的每一条 claim 都应该能对应到 baseline 上的实验信号。

## 复现 Baseline

复现 baseline 是整个 workflow 里最容易被低估的一步。

很多人以为复现只是为了“验证别人论文”，其实不是。复现真正的价值，是为后续所有实验建立一个可靠锚点。你的 idea 到底有没有提升，必须放在同一框架下才能看清楚。

作者起初以为有开源代码就不难复现，但实际会遇到很多问题：

- pip 安装到一半报错。
- 进程直接卡死。
- 主线程不断等待。
- bash 调用需要 sleep，几秒钟的问题会拖成半小时。
- 数据集下载可能因为网络波动中断。
- 模型不知道网络挂了，还会继续假装一切正常。
- 数据路径、GPU 使用文档、环境依赖写在文档里，但 AI 不会主动去读。

作者后来加入了监控：

- 每一步必须实时反馈进度。
- 卡住必须立刻报警。
- 下完必须做指纹校验，确认版本正确。

即便如此，在国内服务器、代理和网络不稳定的情况下，仍然很难一次完全稳健。

### 不要在主线程里跑复现任务

作者强调，复现这类任务不要在主线程里跑。

原因：

- 安装报错、下载日志等会污染上下文。
- 后续对话质量会下降。
- 前台会被长时间等待卡住。
- 原本可以并行的事情会变成串行阻塞。

更好的做法：

- 交给 sub-agent 或 exec 去做。
- 主线程只负责收结果。
- 远程服务器上一定使用 tmux，不要裸 SSH。
- SSH 断开一次连接，整个进程可能就没了。

最稳、最省心的方式，还是先由人把环境和数据准备好，或者 human in the loop 地看着 AI 正确做完，再开一个干净 session，把干净路径直接交给模型。

这样可以避免无休止的上下文污染和隐形时间黑洞。

## 提出 Idea

有了 baseline 和足够的文献积累后，下一步才是提出 idea。

作者最早的做法，是直接让 Claude 基于调研结果想 idea。但 Claude 很难提出真正有 novelty 的 idea，而且它会受自己之前思路、调研笔记、论文摘要、被否掉的想法影响，越到后面越容易陷入已有路径。

后来作者改用 Gemini 做发散：

- Claude 在几个大模型中智力体感最低。
- Gemini 虽然幻觉严重，但经常能提出让人“开脑洞”的 idea。
- Gemini 搜索能力和知识量更强，适合从更干净的起点做广泛发散。

Gemini 拿到的上下文通常只包含：

- 文献调研结果
- baseline 信息

它不知道之前有哪些讨论、哪些想法被否掉，因此能从较干净的起点一次生成多个 idea。图中提到一次生成大概 10 个 idea，每个 idea 带一段简要描述。

## Idea Review

10 个 idea 出来以后，交给 Codex 做独立 review。

作者选择 Codex 的原因：

- review 需要严格智力判断。
- 在上下文确定的情况下，Codex 的判断让作者更放心。
- 关键在于上下文隔离：每个 idea 单独评估。

每个 idea 的 review 维度包括：

- novelty 够不够。
- 技术上是否可行。
- 与 baseline 的兼容性如何。
- 实现复杂度是否可控。
- 预期能带来多大提升。

每个 idea review 完后生成一个 idea card，包括：

- 各维度评分。
- Codex 的判断理由。
- 明确判定：go、revise 或 kill。

最后根据 ranking 选出得分最高的 idea。

选出来后，交给 Claude 拍板。Claude 作为主 architect，不是直接选了就走，而是要回头看 Codex 的意见。如果 Codex 给的是 revise，Claude 需要判断是否采纳修改建议，把 idea 调整到位后再继续推进。

之后，再把 idea 交给 Gemini 做一次针对性文献调研，专门搜索这个方向上是否已有类似工作，以及有没有可打磨润色的空间。通过这一轮验证的 idea，才正式进入实现阶段。

作者尝试过让 Gemini review、让 Claude 和 Codex 互相 review 等策略，最后放弃所有“互相 review”的方案。原因很简单：只要让两个模型互相审，它们永远能提出“还能怎么改进”“还可以从哪个角度优化”。这个过程没有终局，必须由一个角色做最终决定。

最终选出来的 idea，通常是三个模型共同认为在当前上下文里更合适的 idea。

## Research Contract

真正进入实现之前，必须写 research contract。

在动手写任何代码之前，先把所有预期写清楚：

- hypothesis 是什么。
- 什么结果算成功。
- 什么结果算失败。
- 每个 ablation 预期会看到什么。
- 失败信号是什么，而且要独立写，不是“没达到成功”的反面。
- 使用哪些指标 metric。
- 使用怎样的数据 split。
- 每条论文 claim 对应哪条实验信号和哪份实验数据。

这么较真的原因是：如果不在实验之前确定这些东西，模型一定会在结果出来之后帮你合理化。

例如，实验结果差了一点，模型会说“虽然主指标没达标，但从另一个角度看揭示了一些有趣现象”。这种先看到结果再编故事，是科研中最忌讳的。

contract 写好以后，后面所有角色都必须以它为唯一尺子。

写代码时要严格对照 contract 里的：

- 超参数
- metric
- 数据划分
- 成功/失败信号

如果评审结果时发现 contract 不足，必须回头调整 contract。评审结果时先读 contract，再看实验结果，逐条判定每个信号是达标还是没达标。

写论文时，每一条 claim 都必须能对应回 contract 里的某个信号和对应实验数据。

contract 一旦写好，实验开始后不能随意修改。如果确实需要修正，必须产出一个 v2，写清楚改了什么、为什么改。不能事后调整 success signal 去配合结果。



## 可复用清单

### 开始前

- [ ] 明确研究方向和问题。
- [ ] 收集已有文献与 baseline 候选。
- [ ] 确定模型分工：谁搜索、谁发散、谁 review、谁拍板。
- [ ] 给每个任务建立干净上下文。

### 文献调研

- [ ] 优先读 LaTeX / Markdown 源文件，而不是整篇 PDF。
- [ ] 调研阶段优先看摘要和 introduction。
- [ ] 让 scout agent 聚焦中高水平顶会与开源代码。
- [ ] 不让 Zotero 阅读历史限制 idea 来源。

### Baseline

- [ ] 先确定 setting。
- [ ] 先确定 baseline。
- [ ] 先复现 baseline，再讨论 idea。
- [ ] 把复现环境、数据路径、GPU、依赖写清楚。

### 实现与实验

- [ ] 长任务不要塞进主线程。
- [ ] 远程服务器使用 tmux。
- [ ] 每一步有实时反馈。
- [ ] 卡住报警。
- [ ] 下载完成做指纹校验。
- [ ] 避免日志和报错污染主上下文。

### Idea

- [ ] 让 Gemini 或类似强搜索模型做发散。
- [ ] 让 Codex 或类似强判断模型独立 review。
- [ ] 每个 idea 单独评估，避免互相污染。
- [ ] 输出 idea card，并给出 go / revise / kill。
- [ ] 选中 idea 后再做一次针对性文献调研。

### Research Contract

- [ ] 写清 hypothesis。
- [ ] 写清成功标准。
- [ ] 写清失败信号。
- [ ] 写清每个 ablation 的预期。
- [ ] 写清 metric。
- [ ] 写清 data split。
- [ ] 把每条论文 claim 映射到实验信号和实验数据。
- [ ] 实验开始后不随意改 contract；确需修改则生成 v2。

# 第一版锐评

> 评审对象：`researchclaw/` 第一版（mock 闭环 demo）。
> 依据：`科研助手架构批判分析.md`、`OpenClaw-first-demo开发指南.md`、`researchclaw/README.md`、第一版全部核心代码。
> 验证：`npm test` 15/15 通过；`npm run demo:fixture` 完整跑到 summary。

## 一句话结论

第一版**骨架立住了，灵魂还没装进去**。`contract / state / evidence` 三件事里，contract 和 state 的“形”做出来了，但批判分析反复强调的两个内核——**带回边的状态机**和 **claim→evidence 证据闭环**——恰恰是这一版最薄弱、甚至基本悬空的地方。Milestone 3 达标，但不能把它当成“已经在做科研”。

## 做对了的部分

- 分层边界守得很干净：gateway 不判阶段、workflow 只产 artifact、engine 不直接调模型（orchestrator 全程经 adapter），踩在开发指南 8.1 的硬边界上。
- contract 是真·机器可校验（`contract/schema.js` 逐字段校验 + `contractGate`），不是只有 Markdown。
- evidence store append-only、raw payload 脱敏（`redactSecrets`）、`.researchclaw/` 与 `API.md` 都进了 `.gitignore`，工程卫生合格。
- `demo:fixture` 离线全链路能跑，验收底线达成。

## 主要问题（按严重度排序）

### 1. 把状态机做成了单向 DAG + 死路 `blocked`——正是批判分析点名要避免的事

批判分析最重的一句：“科研不是一次性定死的流程图，而是带回边的状态机：调研→发散→评审→实现→实验→失败后回退。” 开发指南 8.3 transition table 也写了 `baseline gate fail → literature_scouting 或 blocked`、`idea review 全 kill → literature_scouting 或 blocked`。

但代码只实现了“或 blocked”这一半：所有 gate 失败一律 `blockWithoutWrite` → `phase="blocked"`（`orchestrator.js:494`），而 `blocked` **没有任何出口**——`advance()` 的 switch 没有 blocked 分支（会 throw "no runnable workflow step"），`revise()` 只认 contract。结果：任何一步 gate 挂掉，项目就**永久卡死**，只能手删 `state.json`。除 contract revise→draft 外，回边一条都没有。这是最该先还的架构债。

### 2. contract 的 `revise` 是空转，human gate 是装饰性的

`reviseContract`（`contract.js:15`）只做三件事：version+1、改 contract_id、把 feedback 塞进 `human_notes`。它**不重新跑 contractDraft workflow / adapter**，所以 hypothesis / metrics / success_criteria 一字未变。改完的 v2 和 v1 内容相同，再 approve 就过。开发指南 13.1 要的是“产出 contract v2 draft”，当前这条人在回路里的修订是假的。

### 3. claim→evidence 闭环没真正落地——而这是项目的灵魂

批判分析结论句：“真正决定上限的……是证据闭环。” contract 里有 `claim_evidence_map`，但**全链路没有任何地方校验“每条 claim 是否真有对应 evidence”**。`evidenceGate`（`gates.js:98`）只检查 6 个 artifact_ref 是否存在，不检查映射；`summary` 的 `evidence_index` 在 mock 里就是一句写死的话 + 把 ref 罗列一遍（`mock.js:73`）。证据“存下来了”，但“结论必须可追溯到证据”这个硬约束**悬空**。这是 demo 看起来闭环、实则最核心约束没合上的地方。

### 4. gate 不一致 + 校验偏浅

- `idea_generation` 的 `>=3` 门槛直接 inline 写在 `orchestrator.js:388`，其他阶段全走 `gates.js`。架构不统一，改门槛要找两处。
- 多数 gate 只查“非空/数量”，不查内容。`reproductionChecklistGate` 里每个数组写 `["todo"]` 也能过。mock 下无所谓，**接真实模型后这种“形状对、内容空”的输出会直接骗过 gate**。批判分析要的 baseline 复现是“硬门槛”，当前只是“非空门槛”。

### 5. 真实科研价值目前为 0（预期内，但要点明）

只有 mock adapter，`adapters/` 下没有 claude/gemini/codex。contract 除 `topic` 外全部来自 fixture（`mock.js:42` 只 set topic），literature/baseline/idea/review 全是写死 JSON。当前证明的是“状态机骨架能跑通”，**没证明“它能帮上任何真实研究”**。

### 6. OpenClaw 侧的 approve/revise 没接通

gateway 的 `ask-user-question → human_feedback` intent 只记录、不路由（`orchestrator.js:56`）。真正的 approve/revise 只能走 HTTP/panel。“在 OpenClaw 聊天里完成人工确认”这条叙事，实现上做不到。

### 7. 并发下 single source of truth 不成立

`FileEvidenceStore` 对 `state.json` 是无锁 read-modify-write。OpenClaw 的 hook（post-tool-use/stop）和用户的 advance 并发到达时，两个请求各读各写、后写覆盖先写。单用户 demo 没事，但批判分析把 single source of truth 列为重点，这层在并发下保证不了。

## 优化方向与技术路线（按优先级）

### P0 — 接真实模型之前必须先还的架构债

1. **让 `blocked` 可恢复 + 补回边。** gate fail 时优先回退到可重跑阶段（baseline fail → literature_scouting；review 全 kill → idea_generation/literature_scouting），`blocked` 只作“连回退都不行”的兜底，并给它一个 recover 入口（advance 支持从 blocked 重跑，或新增 `/recover`）。这是把“重 DAG”改回“带回边状态机”的核心一刀。
2. **让 `revise` 真重跑。** 把 feedback 作为新 input 再跑一次 contractDraft workflow，产出内容真的不同的 v2。否则 human gate 名存实亡。
3. **把 idea_generation 门槛抽进 `gates.js`，统一 gate 入口。**

### P1 — 把“证据闭环”做实（项目最大价值点）

4. **新增 `claimEvidenceGate`：** summary 前逐条检查 `contract.claim_evidence_map` 每个 claim 的 `required_evidence` 是否都能在已存 artifact 里找到；缺失就不准进 success summary。`evidence_index` 由这个映射真实生成。这一步是把 demo 从“看起来闭环”变成“真闭环”。
5. **gate 从“非空”升级到“最低质量”：** 如 checklist 的 `commands` 要像可执行命令、`expected_metrics` 能对上 `contract.metrics.name`；baseline `selected.paper_id` 必须在 candidates/paper cards 里能找到。

### P2 — 接真实能力（Milestone 4/5）

6. **先做最短的一条真实链路：** 按 `API.md` 接一个真实 Claude adapter，只做 contract draft —— 真实生成 contract → 过现有 contract gate → human approve。用最小成本验证“真实模型 + 机器校验 contract”的价值。
7. **做 per-phase adapter router：** engine 仍不直接调模型，workflow 按 phase 选 adapter（Claude contract / Gemini literature / Codex review / mock），失败 fallback mock。`ModelAdapterRequest` 已带 `phase`，改动集中在 workflow 注入处。
8. **OpenClaw 真联调：** 把 `ask-user-question` 接成 approve/revise 入口，补 smoke test log（Milestone 4 验收项）。

### P3 — 健壮性 / 之后

9. orchestrator 内对同一 `projectId` 串行化 `handle`，消除 state.json 并发覆盖。
10. 拆 `literature_scouting` / `deep_literature_research`，补 implementation/experiment 阶段（8.2 已留 enum 扩展位）。

## 演示时的建议话术

> “这一版证明的是科研流程的**可约束骨架**——OpenClaw 能触发、流程有明确 phase、contract 在实验前用机器校验锁死假设和指标。接下来要补的是两件让它真正可信的事：失败能回退的状态机，和每条结论都能追溯到证据的闭环。”
